<img src="<%= image_path("bg.png")%>" style="width:100% ; position: fixed;"/>
		<div class="modal-dialog" style="top: 50%; position: fixed; left: 50%; transform: translate(-50%, -65%);">
		<p style="text-align:left;font-family: 'Amatic SC', cursive; font-size: 30px; font-weight: 300; color: #FFFFFF;">Sign Up</p>
			<!-- Modal content-->
			<div class="modal-content">
				<div class="modal-body">
          <%= form_with(model: @jio, local: true) do |form| %>
           <% if @jio.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@jio.errors.count, "error") %> prohibited this jio from being saved:</h2>

                <ul>
                <% @jio.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
                </ul>
              </div>
            <% end %>

            <div class="form-group">
              <%= form.label :name %>
              <%= form.text_field :name %>
            </div>

            <div class="form-group">
              <%= form.label :contact_number %>
              <%= form.text_field :contact_number %>
            </div>
            
            <div class="modal-footer">
              <button type="button" class="btn btn-default" data-dismiss="modal" href="<%= root_path %>">Back</button>
              <div class="actions">
                <%= form.submit "Comfirm", class: "btn btn-default" %>
              </div>
            </div>
          <% end %>
				
				</div>
			</div>
		</div>
		
		<div class="icon">
			<img src="<%= image_path("pplw.png")%>" style="width:10%"/>
			<p>Sign Up</p>
		</div>
		
		<div class="footer">
			<p>
				InstantVite | FutureHacks 2018
			</p>
		</div>
		
		
	</div>